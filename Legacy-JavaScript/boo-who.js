function booWho(bool) {
  return bool === true || bool === false;
}

booWho(null);
console.log(booWho(null))
console.log(booWho(true))
console.log(booWho(false)) 

// Бу, кто?
// Проверьте, классифицируется ли значение как примитивное логическое значение. Верните trueили false.

// Булевы примитивы — это trueи false.

