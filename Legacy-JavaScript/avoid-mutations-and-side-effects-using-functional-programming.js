// The global variable
let fixedValue = 4;

function incrementer() {
  // Only change code below this line
let copuFixedValue = fixedValue
return copuFixedValue + 1
  // Only change code above this line
}

// Заполните код функции incrementerтак, чтобы она возвращала значение глобальной переменной, fixedValueувеличенное на единицу.

// Ожидающий :1. Ваша функция incrementerне должна изменять значение fixedValue(которое равно 4).
// Ожидающий :2. Ваша incrementerфункция должна возвращать значение, на единицу большее указанного fixedValueзначения.
// Ожидающий :3. Ваша incrementerфункция должна возвращать значение, основанное на fixedValueзначении глобальной переменной.