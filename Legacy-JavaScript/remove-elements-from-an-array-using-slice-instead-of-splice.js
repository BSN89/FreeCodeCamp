function nonMutatingSplice(cities) {

  return cities.slice(0,3);
}

// Перепишите функцию nonMutatingSplice, используя sliceвместо splice. Она должна ограничить предоставленный citiesмассив длиной 3 и вернуть новый массив только с первыми тремя элементами.

// Не изменяйте исходный массив, предоставленный функции.