function Dog(name,color) {
this.name = name
this.color = color
this.numLegs = 4
}

const terrier = new Dog('Charly', 'red')

console.log(terrier)

// Создайте еще один Dogконструктор. На этот раз настройте его на прием параметров nameи colorи зафиксируйте свойство numLegsна 4. Затем создайте новый Dogсохраненный в переменной terrier. Передайте ему две строки в качестве аргументов для свойств nameи color.

