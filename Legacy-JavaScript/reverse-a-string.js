function reverseString(str) {
  return str.split('').reverse().join('');
}

reverseString("hello");

// Перевернуть предоставленную строку и вернуть перевернутую строку.

// Например, "hello"должно стать "olleh".